const MessageForm = () => {
    return (
        <div>
            <form className="message-form" onSubmit={handleSubmit}>
                
            </form>
        </div>
    );
};

export default MessageForm;